EXPR
pos0 codiceC e pos1 idfunzione se è callproc o null

CONCATENAZIONE FILE FINA

ANALISI SEMANTICA
vardeclop -> idlistinitop può avere o non l'expr quindi il rowtable che ritorna idlistinit a vardeclop avrà il gettype
settato SE expr è presente altrimenti null




TODO
strcat -> se passate due stringConst ad una funzione, strcat al suo interno non è possibile, quindi prima
di passare le due costanti stringhe, vanno incapsulate in una variabile.

plusOp non consentito se il primo operando è un stringCOnst

assignOp string = strcat -> da risolvere
