string l, ll, lll, firstString, secondString;

proc j()float, int:
    int n, y, z := 0;
    float zz, yy := 5.3;
    string s,s2,s3;
    zz := 5.9;
    y := 10;
    n := zz + y;
    yy := y + zz;
    s := "ciao";
    write(s);
    write(yy);
    write(n);

   -> yy,n
corp;

proc x()void :
	int x, b, c:= -2;
	int main;
	string strNull := null;
	string eee := "Ciao";
	string str1 := "str1";
	int n:= 5+4+3;

		->
corp;

proc getFirstString(string s) string:

-> s
corp;

proc getSecondString(string s) string:

-> s
corp;

proc main() void:

  float f;
  int n, i := 1;
  l,ll,lll := "l1","l2","l3";
  write(l, ll, lll, "\n");
  f,n := j();
  write(f,n);

  while i <> 0 do
    write("Inserisci prima stringa: ");
    readln(l);
    write("Inserisci seconda stringa:   ");
    readln(ll);
    if l > ll then
        write("Stringa ", l," > della seconda stringa ", ll, "\n");
    elif l < ll then
        write("Stringa ", l," < della seconda stringa ", ll, "\n");
    else write("Stringa ", l," = alla seconda stringa ", ll, "\n");
    fi;
    write("Inserisci 0 per interrompere l'esecuzione di questo programma oppure un qualsiasi altro numero per continuare:    ");
    readln(i);
  od;

  if getFirstString("ciao") > getSecondString("hola") then
    write("Ciao > Hola\n");
  else write ("Ciao < Hola \n");
  fi;
->
corp;

